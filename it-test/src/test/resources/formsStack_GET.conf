include classpath("formsStack.conf")

### Override Example Adapter Configuration ###
config.formsAdapter {
  options.config {
    address = ${global.address.forms.example.adapter}
    testStrategy = GET_REQUEST
  }
}
